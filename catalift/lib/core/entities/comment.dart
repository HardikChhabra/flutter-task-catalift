// comment entity

class Comment {
  String commentId;
  String userId;
  String postId;
  String content;
  String timStamp;

  Comment(
      this.commentId, this.userId, this.postId, this.content, this.timStamp);
}
