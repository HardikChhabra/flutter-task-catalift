// comment entity

class Comment {
  String commentId;
  String userId;
  String postId;
  String content;
  String timeStamp;

  Comment(
      this.commentId, this.userId, this.postId, this.content, this.timeStamp);
}
